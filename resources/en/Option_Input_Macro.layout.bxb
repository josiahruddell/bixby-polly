layout-macro-def(option-input-macro) {
  params {
    param (option) {
      type (Option)
      min (Required)
      max (One)
    }
  }

  content {
    title-card: title-area {
      slot1: text("#{value(option.text)}")
    }
  }
}
